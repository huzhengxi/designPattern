package com.jason.design.pattern.behavioral.iterator;

/**
 * 迭代器模式
 * 定义：提供一种方法，顺序访问一个集合对象中的各个元素，而不暴露该对象的内部表示
 * 类型：行为型
 * <p>
 * 适用场景：1.访问一个集合对象的内容而无需暴露她的内部表示
 * 2.为遍历不同的集合提供一个统一的接口
 * <p>
 * 优点：1.分离了集合对象的遍历行为
 * <p>
 * 缺点：1.类的个数成对增加
 */
public class Test {
}
